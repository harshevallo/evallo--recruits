/**
 * Pure helpers safe in both browser and Node.
 *
 * Nothing environment-specific: no window, document, process, or fs. Utilities that need a
 * browser belong in apps/web/src/utils; server-only helpers belong in apps/api/src/lib.
 *
 * Planned: slug · dates · strings · format
 */

export {};
